image: cimg/openjdk:17.0.2

pipelines:
  branches:
    master:
      - step:
          caches:
            - gradle
          script:
            - make buildw
  custom:
      renovate:
        - step:
            image: renovate/renovate
            caches:
              - maven
            script:
              - export RENOVATE_CONFIG=$(< renovate.json)
              - renovate
